export enum SlackIcons {
  FAILURE = ':fire:',
  SUCCESS = ':white_check_mark:',
  MAGNIFIER = ':mag:',
  PULL_REQUEST = ':pullrequest:',
  DOWNLOADS = ':scroll:',
}

export enum GithubStatus {
  SUCCESS = 'success',
  FAILURE = 'failure',
}

export enum Colors {
  SUCCESS = '#28A745',
  FAILURE = '#CB2431',
}
